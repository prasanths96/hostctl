{
    "bimt-host-endpoint": "http://localhost:8090/deployer/status",
    "commands": [
        {
            "dir": "", 
            "command": ["echo", "commands involving | & >> should be avoided"]
        },
        {
            "dir": "<hlfdpath>", 
            "command": ["sudo", "./hlfd", "install", "prereqs"]
        },
        {
            "dir": "<hlfdpath>", 
            "command": ["sudo", "./hlfd", "deploy", "ca", "-n", "first-ca", "-p", "8040"]
        }
    ]
}